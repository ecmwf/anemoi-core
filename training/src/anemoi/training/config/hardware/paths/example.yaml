data: ???
output: ???

truncation: null
logs:
  base: ${system.paths.output}logs/
  wandb: ${system.paths.logs.base}wandb/
  mlflow: ${system.paths.logs.base}mlflow/
  tensorboard: ${system.paths.logs.base}tensorboard/
checkpoints: ${system.paths.output}checkpoint/
plots: ${system.paths.output}plots/
profiler: ${system.paths.output}profiler/
graph: ${system.paths.output}graphs/
warm_start: null

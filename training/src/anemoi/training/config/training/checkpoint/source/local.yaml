# Checkpoint source — read a checkpoint from an explicit file on the local
# filesystem. `~` and relative paths are expanded.
_target_: anemoi.training.checkpoint.sources.local.LocalSource
path: null   # REQUIRED: the checkpoint file to load (e.g. training/checkpoint/source=local training.checkpoint.source.path=/path/to/model.ckpt)

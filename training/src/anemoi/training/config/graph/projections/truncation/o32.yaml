---
defaults:
  - _self_

truncation:
  # o32 truncation grid with KNN=32. Sigma is approximately four o32 grid spacings
  # (~1200 km, in radians).
  grid: o32
  edge_weight_attribute: gauss_weight
  gaussian_norm: l1
  num_nearest_neighbours: 32
  sigma: 0.18840

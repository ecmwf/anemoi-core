dataset: ???
graph: ???
# To load the graph from a file, you need to provide a path here like: /path/to/your/graph.pt
# If no such file exists, the graph will be created from the config.
loss_matrices_path: null    # base directory for multiscale smoothing matrix files
truncation_up_file_path: null    # .npz file for truncation up-projection (data → coarse)
truncation_down_file_path: null  # .npz file for truncation down-projection (coarse → data)
warm_start: null

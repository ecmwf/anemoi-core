prefetch_factor: 2
pin_memory: True

# ============
# read_group_size:
#   Form subgroups of model comm groups that read data together.
#   Each reader in the group only reads 1/read_group_size of the data
#   which is then all-gathered between the group.
#   This can reduce CPU memory usage as well as increase dataloader throughput.
#   The number of GPUs per model must be divisible by read_group_size.
#   To disable, set to 1.
# ============
read_group_size: ${hardware.num_gpus_per_model}

num_workers:
  training: 8
  validation: 4
  test: 4
batch_size:
  training: 1
  validation: 1
  test: 4

# ============
# Default effective batch_size for training is 16
# For the o96 resolution, default per-gpu batch_size is 2 (8 gpus required)
# The global lr is calculated as:
# global_lr = local_lr * num_gpus_per_node * num_nodes / gpus_per_model
# Assuming a constant effective batch_size, any change in the per_gpu batch_size
# should come with a rescaling of the local_lr to keep a constant global_lr
# ============

# runs only N training batches [N = integer | null]
# if null then we run through all the batches
limit_batches:
  training: null
  validation: null
  test: 20

#  set a custom mask for grid points.
# Useful for LAM (dropping unconnected nodes from forcing dataset)
grid_indices:
  _target_: anemoi.training.data.grid_indices.FullGrid
  nodes_name: ${graph.data}

# ============
# Dataloader definitions
# These follow the anemoi-datasets patterns
# You can make these as complicated for merging as you like
# See https://anemoi-datasets.readthedocs.io
# ============

dataset: ${hardware.paths.data}/${hardware.files.dataset}
reorder_list: {'10u': 0, '10v': 1, '2d': 2, '2t': 3, 'cos_julian_day': 4, 'cos_latitude': 5, 'cos_local_time': 6, 'cos_longitude': 7, 'cp': 8, 'hcc': 9, 
 'insolation': 10, 'lcc': 11, 'lsm': 12, 'mcc': 13, 'msl': 14, 'q_100': 15, 'q_1000': 16, 'q_150': 17, 'q_200': 18, 'q_250': 19, 'q_300': 20, 'q_400': 21,
 'q_50': 22, 'q_500': 23, 'q_600': 24, 'q_700': 25, 'q_850': 26, 'q_925': 27, 'sin_julian_day': 28, 'sin_latitude': 29, 'sin_local_time': 30, 'sin_longitude':
 31, 'skt': 32, 'sp': 33, 'ssrd': 34, 'strd': 35, 't_100': 36, 't_1000': 37, 't_150': 38, 't_200': 39, 't_250': 40, 't_300': 41, 't_400': 42, 't_50': 43,
 't_500': 44, 't_600': 45, 't_700': 46, 't_850': 47, 't_925': 48, 'tcc': 49, 'tcw': 50, 'tp': 51, 'u_100': 52, 'u_1000': 53, 'u_150': 54, 'u_200': 55,
 'u_250': 56, 'u_300': 57, 'u_400': 58, 'u_50': 59, 'u_500': 60, 'u_600': 61, 'u_700': 62, 'u_850': 63, 'u_925': 64, 'v_100': 65, 'v_1000': 66, 'v_150': 67,
 'v_200': 68, 'v_250': 69, 'v_300': 70, 'v_400': 71, 'v_50': 72, 'v_500': 73, 'v_600': 74, 'v_700': 75, 'v_850': 76, 'v_925': 77, 'w_100': 78, 'w_1000': 79,
 'w_150': 80, 'w_200': 81, 'w_250': 82, 'w_300': 83, 'w_400': 84, 'w_50': 85, 'w_500': 86, 'w_600': 87, 'w_700': 88, 'w_850': 89, 'w_925': 90, 'z': 91,
 'z_100': 92, 'z_1000': 93, 'z_150': 94, 'z_200': 95, 'z_250': 96, 'z_300': 97, 'z_400': 98, 'z_50': 99, 'z_500': 100, 'z_600': 101, 'z_700': 102, 'z_850': 103, 'z_925': 104}

training:
  dataset: ${dataloader.dataset}
  frequency: ${data.frequency}
  start: 2016-01-01
  end: 2022-05-31
  reorder: ${dataloader.reorder_list}
  drop: ['u_10', 'v_10', 'w_10', 'z_10', 'q_10', 't_10']

validation_rollout: 1 #  number of rollouts to use for validation, must be equal or greater than rollout expected by callbacks

validation:
  dataset: ${dataloader.dataset}
  frequency: ${data.frequency}
  start: 2022-06-01
  end: 2023-05-31
  reorder: ${dataloader.reorder_list}
  drop: ['u_10', 'v_10', 'w_10', 'z_10', 'q_10', 't_10']

test:
  dataset: ${dataloader.dataset}
  start: 2022
  end: null
  frequency: ${data.frequency}
  drop:  []
